type User {
  id: ID!
  name: String!
  email: String!
  ratedIn: [Rating!]!
  authorOf: [Rating!]!
  createdAt: String!
  updatedAt: String!
}

type Query {
  user(id: ID!): User
}

type Mutation {
  signup(name: String!, email: String!, password: String!): AuthPayload
}
